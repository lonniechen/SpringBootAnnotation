package springBootApplication;

/**
 * @author Lang
 *
 *	@SpringBootAnnotation = @EnableAutoConfiguration + @Configuration + @ComponentScan
 *	Here @ComponentScan uses the default setting, which scan the current package
 *	Note:
 *	@Configuration focus on the @Bean in the project, while @EnableAutoConfiguration is more on the importing jar imported from the dependencies (just a guess, maybe wrong)
 *
 */
public class Info {

}
